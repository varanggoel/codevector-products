{
  "name": "codevector-products",
  "version": "1.0.0",
  "description": "Fast, consistent product browsing over ~200k rows using keyset pagination (Node.js + Neon Postgres).",
  "type": "module",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "seed": "node scripts/seed.js",
    "insert-burst": "node scripts/insert-burst.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "pg": "^8.12.0"
  }
}
